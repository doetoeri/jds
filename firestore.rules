rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This rule allows anyone to read and write to the database.
    // It should be secured after stabilizing the signup functionality.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
